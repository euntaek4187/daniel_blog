{
  "name": "daniel_blog",
  "version": "1.0.0",
  "description": "daniel's blog",
  "main": "index.js",
  "scripts": {
    "build": "pip install --upgrade pip setuptools && pip install --no-cache-dir -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "dependencies": {},
  "devDependencies": {}
}
